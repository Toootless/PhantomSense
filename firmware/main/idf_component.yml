idf_component_register(
    SRCS "main.c" "app_config.c" "wifi_setup.c" "mqtt_setup.c"
    INCLUDE_DIRS "include"
    REQUIRES esp_wifi esp_netif nvs_flash mqtt_client tinyml_client
    PRIV_REQUIRES esp_event esp_system nvs_flash log
)
