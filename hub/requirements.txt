# PhantomSense Hub - Python Dependencies (Windows-Friendly)
# NOTES FOR WINDOWS USERS:
# 1. Use quick_install.bat for pre-built wheels only (recommended)
# 2. OR install C build tools (Visual Studio Build Tools, MSVC compiler)
# 3. For numpy only from wheels: pip install --only-binary=:all: numpy
#
# Pre-built wheels available for: numpy, scipy, matplotlib, PyQt6, etc.
# Use: pip install --prefer-binary -r requirements.txt
#
# If you get "Unknown compiler" error: Install Microsoft Visual C++ Build Tools

# Numeric computing (pre-built wheels available)
# Use: pip install --only-binary=:all: numpy
numpy>=1.26.0,<2.0

# Core async framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# MQTT broker and client
paho-mqtt==2.1.0
aiomqtt==2.5.1

# LLM Integration
ollama==0.1.0

# GUI & Visualization (pre-built wheels available)
PyQt6>=6.6.0
matplotlib>=3.8.0

# Web APIs
requests==2.31.0
httpx>=0.25.2

# Data logging & persistence
python-dotenv==1.0.0
pyyaml==6.0.1

# Monitoring & metrics (pre-built wheels available)
prometheus-client==0.18.0
psutil>=5.9.6

# Logging & debugging
python-json-logger==2.0.7
colorlog==6.8.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code quality
black==23.12.0
flake8==6.1.0
mypy==1.7.1
isort==5.13.2
